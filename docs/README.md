# Documentation Notes

This `docs/` folder contains lightweight repository documentation:

- `data_README.md`: external data requirements and access notes
- `requirements.txt`: Python package requirements used for the analysis scripts

The main repository overview, manuscript-package structure, code entry points, and reproducibility notes are documented in the repository-root `README.md`.

